# SapVault 待办（1.2.x）

这份清单是**唯一**的待办来源：还没做的事全部收在下面的 1.2.x 规划里，
已经做完的按版本归档在文件最后（带提交号与标签，方便回溯）。

每条都写清「现象 → 证据 → 建议做法」，可以直接开工。

**版本规划（2026-09-21 更新）**

| 版本 | 主题 | 内容 |
| --- | --- | --- |
| **1.2.1** | 紧急修复 ✅ | 表格形式文件（.env / TOML / INI …）里的账号选择器无法选择 —— 见文件末尾的归档 |
| **1.2.2** | 紧急修复 ✅ | 下拉框内滚动会自动关闭、文件跳转落到“文档”目录 —— 见文件末尾的归档 |
| **1.2.3** | UI 修复收尾 ✅ | 同步页键搜索 + `Ctrl+K`、导入保险库改用自家弹窗、模态框焦点陷阱、编辑条目分区折叠、表单控件对齐，外加「编辑条目备注丢失」与下拉框样式统一 —— 见文件末尾的归档 |
| **1.2.4** | 数据安全 | 单实例运行（原 19）、`with_vault_mut` 事务性（原 16） |
| **1.2.5** | 自定义标题栏 | 与应用风格统一，并承载锁定 / 剪贴板倒计时等（原 18） |
| **1.2.6** | 清理与细节 | 死代码与无用状态（原 13）、新建保险库页面的无关设置（原 14）、账号列表键盘导航（原 15）、界面文案统一（原 17） |
| **2.0.0** | 前端 TypeScript 化 | 类型化的 IPC 契约 / 状态 / DOM 工厂，把「字段名写错、漏处理分支、属性写错」挡在编译期 —— 见下方 2.0.0 段 |

编号里的「原 N」指的是本次重构之前的条目号，提交信息里引用过这些号，保留以便回溯。

---

## 1.2.4 数据安全

### 单实例运行：第二次启动唤起已打开的窗口（原 19）

- 现状：可以同时打开多个 SapVault 窗口，而且**必须**限制 —— 两个窗口共享同一个保险库文件，
  保存时各自拿着自己内存里的快照整份回写（`state.rs::with_vault_mut` → 整份重新加密写盘），
  于是「A 窗口保存」会把「B 窗口改过但还没保存的内容」覆盖掉；锁定状态、空闲计时、剪贴板
  自动清空也各算各的，容易让人分不清哪个是"真的"。
- 期望行为：同一时间只允许一个实例；已经打开时再次双击图标 / 快捷方式 **不再新开窗口，
  而是把已有窗口唤到前台并聚焦**（最小化状态先还原，在后台则激活）。
- 技术方案（Tauri v2）：
  1. **官方插件（推荐）**：`tauri-plugin-single-instance`。在 `lib.rs` 里
     `.plugin(tauri_plugin_single_instance::init(|app, argv, cwd| { … }))`，
     回调里对主窗口依次 `show()` → `unminimize()` → `set_focus()`（必要时短暂置顶再取消，
     当作"窗口在这里"的提示）。注意它**必须最先注册**（在其他插件之前），前端不需要额外权限声明。
  2. 备选：自己用命名互斥体（`CreateMutexW` 后判断 `ERROR_ALREADY_EXISTS`）判断重复启动，
     再用 `FindWindowW` + `SetForegroundWindow` 唤起已有窗口 —— 不引依赖，但要多写 Win32 代码。
- 实现时要处理的细节：
  - 第二实例带命令行参数时（将来可能扩展成"打开某个条目 / 同步某个文件"），把参数转交给第一实例执行。
  - Windows 的前台窗口锁定策略可能让 `SetForegroundWindow` 静默失败，
    需要配合 `AllowSetForegroundWindow` 或"短暂置顶 + 取消"的写法
    （SAP GUI 登录窗口那条已经用过同样的手法，见 `sapgui.rs`）。
  - 与未来的"最小化到托盘"配合：唤起时要从托盘还原窗口。
  - 唤起窗口 ≠ 解锁：仍然要求重新输入主密码（空闲自动锁定的语义不能变）。
  - 便携模式按什么判定"同一个应用"：建议以**数据目录**为判定键（而不是可执行文件路径），
    这样同一个 exe 指向不同数据目录的两个便携实例仍可同时运行，而共享同一保险库的实例会被拦住。
- 验收：连续双击两次只出现一个窗口；第二次启动时已有窗口被激活（最小化状态会还原）；
  两个不同数据目录的便携实例仍可各自运行；用审计 / 手动确认锁定时长与剪贴板倒计时不再被多实例干扰。

### `with_vault_mut` 事务性（原 16）

- 现象：闭包返回 `Err` 时不会回滚内存里的改动，也不会落盘（当前调用方都是先校验后修改，
  所以还没出问题，但这是往后加功能很容易踩的坑）。
- 证据：`src-tauri/src/state.rs` 的 `with_vault_mut()`。
- 建议：改成「校验 → 克隆 → 提交」，或让闭包只声明变更。
- 为什么和单实例放一起：两者都是"同一份保险库只允许一个真相"这条线上的事；
  1.1.2 已经补上多窗口之间最容易踩的坑之外的几处，这两条是剩下的。

---

## 1.2.5 自定义窗口标题栏（原 18）

### 自定义窗口标题栏（与应用风格统一）

- 现状：窗口用的是 Windows 自带的标题栏和最小化／最大化／关闭按钮，深色主题下与应用整体风格
  对不上（按钮是系统配色，标题栏底色也与侧边栏、主区不一致）。
- 目标：把标题栏纳入应用自己的视觉语言（同一套圆角、hover／按下反馈、图标线宽与颜色），
  **不牺牲系统行为**（窗口吸附、Win11 Snap 布局、阴影、贴边、多显示器、DPI 缩放）。
- 技术方案（Tauri v2，按推荐顺序）：
  1. **overlay 模式（建议先试）**：`tauri.conf.json` → `app.windows[0].titleBarStyle: "overlay"`，
     内容延伸到标题栏区域，**系统按钮仍然保留** —— 吸附、Snap 布局、阴影、贴边全部不受影响，
     我们只负责标题栏区域的配色与交互。改动最小、风险最低。
  2. **完全自绘**：`decorations: false`，自己画三个按钮并调用 `appWindow.minimize()` /
     `toggleMaximize()` / `close()`；拖动区用 `data-tauri-drag-region`（或 `startDragging()`），
     双击标题栏切换最大化，监听 resize 切换"最大化 / 还原"图标。
     **代价**：Win11 上"鼠标悬停最大化按钮弹出 Snap 布局"会丢失，要靠 `WM_NCHITTEST` /
     `HTMAXBUTTON` 的非客户区命中测试才能找回来；贴边、多显示器拖拽都要重新验证。
  3. 需要的窗口权限（`src-tauri/capabilities/default.json`）：
     `core:window:allow-minimize`、`core:window:allow-toggle-maximize`、`core:window:allow-close`、
     `core:window:allow-start-dragging`。
- 验收清单：最小化／最大化／还原／关闭、双击标题栏、拖拽移动、贴边、Snap 布局、
  DPI 100%/125%/150%、多显示器、最大化状态下的按钮位置与悬停反馈；
  并给 `tools/audit-ui.ps1` 加一条「标题栏高度 + 三个按钮命中区」的几何断言。
- 统一标题栏之后，这一条还能放什么（按价值排序，建议分批做）：
  1. **锁定按钮 + 剪贴板倒计时**：最贴合这个应用 —— 一眼看到「还有 12 秒清空剪贴板」，
     点一下立即锁库（现在要滚到侧边栏底部才能锁）。
  2. **同步状态徽标**：还有 N 处密码待同步，点一下直接进「同步文件」页。
  3. **搜索框**：把账号页的搜索挪进标题栏，列表区多出一整行给内容。
  4. **保险库标识**：当前保险库名 + 便携／本地模式，多保险库或 U 盘场景很有用。
  5. **置顶开关**（always on top）：抄密码、对照文档时实用。
  6. **主题快速切换**：目前藏在侧边栏品牌区的图标里，要悬停才看得懂。
  7. **SAP 登录状态**：是否检测到 SAP GUI、上次登录的系统与时间。
  8. **紧凑模式**开关：缩小行高，小屏幕上多显示几条。
  9. **应用菜单**（关于、快捷键说明、打开数据目录）：「数据目录」现在锁定页和设置页各有一处。
- 注意：标题栏要与「拖拽上传的整屏投放层」「右键菜单」「模态框」的层级配合好 ——
  拖动区不能吃掉按钮点击，也不能挡住投放层。

---

## 1.2.6 清理与细节

### 死代码与无用状态（原 13）

- `ui/js/state.js` 的 `state.presets` 从 `info.presets` 取值，但后端 `Bootstrap` 结构体里
  没有这个字段，永远是 `undefined`。要么补后端，要么删掉。
- `ui/js/views/accounts.js` 导入了 `formatBytes`、`openFileKeys` 但没使用。
- `api.js` 里的 `entrySummary`、`vaultStatus`、`paths` 没有任何调用点。
- `commands.rs` 的 `clipboard_clear` 把剪贴板逻辑又抄了一遍，`clipboard.rs` 里已有现成封装。

### 新建保险库页面上的无关设置（原 14）

- 现象：首次创建保险库的界面上有「删除条目时二次确认」复选框，而设置页里也有同一个开关。
- 证据：`ui/js/views/lock.js` 的 `createForm()`。
- 建议：去掉，或移到设置页。

### 账号列表的键盘导航与语义（原 15）

- 现象：`role="button"` 的行只处理 Enter/Space，没有上下方向键；也没有 `aria-selected`。
- 建议：加方向键移动选中项，并把 `role` 换成 `listbox/option` 组合。

### 界面文案里的「SAP 换行格式」说明统一（原 17）

- 「复制用户名 + 密码」在两个页面有三处不同长度的手写说明，
  可以统一成一句带链接的说明（SAP 登录相关的文案在 1.1.1 已经重写过一轮，剩下这几处）。

---

## 2.0.0 前端 TypeScript 化（规划）

**现状**：前端是零依赖的原生 ES Module，没有类型、没有构建步骤 —— 12 个 `ui/js/*.js`（2276 行）
加 7 个 `ui/js/views/*.js`（4102 行），靠 `tools/` 的排版审计与交互回归兜底。
这个仓库**不打算改**「运行期零依赖」这条（见 AGENTS.md 铁律 2）：TypeScript 只进构建期。

**为什么值得做**（下面每一条都是真出过的 bug，不是假想）：

1. `h()` 的 props 没有类型 → `<textarea value="…">` 这种「写了个不存在的属性」只能等用户发现
   —— 1.2.3 的「编辑条目备注丢失」正是它。
2. `state` 没有类型 → `state.presets` 从 `info.presets` 取值，但后端 `Bootstrap` 里从来没有这个字段，
   永远是 `undefined`，一直没人发现（1.2.6 原 13）。
3. 视图与快捷键靠字符串约定 → `Ctrl+K` 聚焦 `#sap-filter`，而整个前端没有这个 id（1.2.3 原 3）。
4. 前后端字段契约只能靠 Rust 侧的手写断言 + 前端自觉 → 详情面板读顶层 `entry.systemId`（只有列表摘要才有）
   那次就是这样漏的。

**分三步做，每一步都能单独发布**：

1. **先开类型检查，不动文件后缀**：加 `tsconfig.json`（`allowJs` + `checkJs` + `strict` +
   `noUncheckedIndexedAccess`），TypeScript 只作为 `devDependencies`；
   按需在文件顶加 `// @ts-check` 并用 JSDoc 补注解。这一步不需要重写就能抓到上面第 1、2 类问题。
   （不想引 npm 的话，等价方案是 `deno check`；但仓库里已经有 Node，一个 dev 依赖更省事。）
2. **逐文件 `.js` → `.ts`**，顺序按依赖走：`format/icons/dom` → `api/state/modal/combobox` → `views/*`。
   源码放 `ui/src/`，`tsc` 输出到 `ui/js/`（ESM，一进一出，**不上打包器**），
   `index.html` 与 `tools/serve-ui.mjs` 继续加载构建产物；`cargo tauri build` 挂
   `beforeBuildCommand` 先跑一次编译。审计脚本与交互用例同时改成 `.ts`，直接复用前端的类型与选择器常量。
3. **补上契约这块拼图**：给 `commands.rs` / `model.rs` 加类型导出（`ts-rs` 或手写生成器），
   产出 `ui/src/types/ipc.ts`；现有那个「前后端字段契约」单测改成校验生成结果 ——
   以后字段名写错，`cargo test` 就会红。

**类型化之后顺手能加的功能**（按价值排序，都依赖第 1–3 步）：

1. **IPC 契约自动化**：命令签名或结构体一改就重新生成类型，前端调用点与 Rust 端不可能再对不上。
2. **视图与快捷键自洽**：`view` 用联合类型 + `Record<View, string>` 的筛选框注册表，
   新增视图忘了配 `Ctrl+K` 落点会直接编译失败。
3. **`h()` 的类型化重载**：`h("textarea", { value })`、`h("input", { type: "checkbox", checked })`
   走正确的属性路径，属性名写错编译期就报（备注丢失那类 bug 从此不会复现）。
4. **错误分类穷尽处理**：`describeError()` 的 `kind`（password-rule / password-cycle / …）用联合类型，
   新增一类错误却忘了处理分支会报错（目前是静默落到通用 toast）。
5. **持久化格式版本化**：`vault.sapvault` 与 `settings.json` 加 `schema_version` + 迁移函数，
   老文件自动升级、导入不兼容文件时给明确原因（适合和 2.0.0 一起发，与前端无关但同批交付）。
6. **审计断言复用类型**：几何断言与交互用例引用视图导出的选择器常量（如 `VIEW_SELECTORS`），
   改类名时审计不会静默失效（现在审计里的选择器全是手写字符串）。

**验收**：`tsc --noEmit` 零错误、`cargo test` 全绿、`pwsh tools/audit-ui.ps1 -WithModals -Interactions` 全绿，
并且 `cargo tauri build` 产出的 exe 体积不因类型化变大（类型只存在于构建期）。

---

## 已完成（按版本归档）

### 1.2.3 —— 未打标签（UI 更新后可能还有 bug，先不 tag）

- **修复：编辑条目时备注丢失。** `ui/js/dom.js` 的 `h()` 把 `value` 当属性写
  （`setAttribute("value", …)`），而 `<textarea>` 没有这个内容属性 —— 备注框渲染出来是空的，
  保存时又把空值写回保险库，备注就没了。现在 `value` 直接赋值给元素的 `el.value`
  （`input` / `textarea` / `select` 都是这一条路径）。
- **下拉框样式统一。** `ui/js/combobox.js` 新增 `selectMenu()`：与「同步文件」页的账号选择器
  同一套控件与面板，只是**不带搜索框**（改为输入首字母跳转）。「编辑条目」里的
  **分类**与 **SAP Logon 系统**两个原生 `<select>` 都换上它（id 仍是 `editor-category` /
  `editor-sap-system`，面板挂在 `#combo-root` 下，弹窗内也不会被裁切）。
- **同步文件页的键搜索 + `Ctrl+K`（原 3）。** `bindShortcuts()` 以前聚焦 `#sap-filter`，
  而这个 id 整个前端都不存在，所以除了账号页按 `Ctrl+K` 什么都没发生。现在：
  账号页 → `#global-search`、同步文件页 → `#sync-key-filter`（新增过滤框，按键路径或值过滤，
  过滤时折叠树自动全展开、并显示「N / M 个键匹配」）、关联关系页 → `#assoc-filter`。
- **导入保险库改用自家弹窗（原 6）。** `promptModal()` 重写为「提交时等待后端结果」：
  主密码不对时弹窗保持打开、显示错误并让用户在同一个弹窗里重试，不再是 `window.prompt`
  那个和应用风格完全无关的系统输入框。
- **模态框焦点陷阱（原 7）。** 打开时记录触发元素，`Tab` / `Shift+Tab` 在 `.modal` 内循环
  （焦点被挪到弹窗外时会拉回来），关闭后焦点回到触发按钮。
- **编辑条目弹窗分区折叠（原 8）。** 六个区块变成可折叠面板，默认只展开
  「基本信息」「凭据」；展开状态存在编辑器自己的持久对象里，重渲染不会重置。
- **表单控件对齐（原 9）。** `tokens.css` 新增 `--control-h`（32px）/ `--control-h-sm`（28px），
  `.input` / `.select` / `.textarea` / `.btn` / `.combo__control` 统一高度；
  `.form__grid` 改成明确断点（≥720px 两列、≥1100px 三列）+ `align-items: start`；
  「手动补录」那行用 `.form__grid--action`（`1fr 1fr auto`）把按钮留在同一行。
- **审计与回归。** `tools/inspect-ui.mjs` 新增几何断言：同一个 `.form__grid` 同一行里
  控件的 `top` 与 `height` 差 ≤1px，超了报 FAIL；`tools/check-interactions.mjs` 新增
  备注往返、`Ctrl+K` 落点、焦点陷阱与焦点还原、分类 / 系统下拉、导入弹窗重试等用例
  （`tools/ui-fixtures.js` 的桩增加 `vault_import` 主密码校验与 `setPickFiles`）。
  回归结果：界面审计 119 项、交互回归 58 项全绿。

### 1.2.2 —— 标签 `v1.2.2`

- **紧急修复 1：下拉框内容滚动时面板自动消失。** 面板内部的选项列表是可以滚动的，
  而「滚动就关闭」的监听把面板自身的滚动也算成了外部滚动。
  现在只有面板**之外**的滚动（页面 / 列表跟着移动，锚点跑了）才关闭。
- **紧急修复 2：文件跳转落到了「文档」目录。** `explorer /select,<路径>` 这个参数里只要含空格，
  Rust 就会把**整个参数**（包括 `/select,` 开关本身）用引号包起来，Explorer 解析不了就忽略它，
  于是打开默认目录（文档）。实测确认：`"/select,C:\…\probe file.txt"` → 打开文档；
  `/select,"C:\…\probe file.txt"` → 正确定位。现在改用 `Command::raw_arg()` 精确控制引号，
  目录情形同理（`"<目录>"`）。新增两个单元测试固定这个参数格式。
- 回归用例（tools/check-interactions.mjs）：面板内滚动不关闭、页面滚动会关闭。
  单元测试：86 个（新增 2 个 explorer 参数测试）。
### 1.2.1 —— 标签 `v1.2.1`

- **紧急修复**：同步文件页里表格形式的内容（`.env`、TOML、INI、properties、tfvars 等）无法选择账号。
  两个原因：① 选择器面板渲染在表格单元格里，被 `.table__cell` / `.table` 的 `overflow: hidden` 裁掉，
  看起来就像禁用；② 面板每次重绘都会重新创建并追加，旧的没有移除，导致面板叠层、点击落到过期的那一层。
  现在面板挂在 `<body>` 下的独立宿主里、用 `position: fixed` 定位（任何祖先都裁不到它，
  空间不够时自动向上展开），并且每次重绘前先移除旧面板。
- 回归用例：表格里的选择器必须「面板可命中（`elementFromPoint` 落在面板内）+ 选中后绑定回显」，
  已经加进 `tools/check-interactions.mjs`。
### 1.2.0 —— 提交 `a51a962`，标签 `v1.2.0`

- **备注里的链接可以直接点**：详情面板的备注中 `http(s)://`、`mailto:` 渲染成可点元素，
  点击交给系统默认浏览器打开；后端新增 `open_link` 命令，只放行这三种协议并拒绝控制字符，
  链接不带 `href`，WebView 不会被导航走。
- **同步文件的账号下拉改成可搜索选择器**（`ui/js/combobox.js`）：输入关键字按
  「账号标题 + 用户名」过滤，支持上下键 / 回车 / `Esc` / 点外部关闭；树与表格共用同一实现。

### 1.1.2 —— 提交 `8e3fdec`，标签 `v1.1.2`

- 切换分类（或输入搜索）后，右侧详情不再显示已不在列表里的账号（原 1 / 2 的同源问题一并处理）。
- 「同步文件」区块只对 SAP 账号显示；非 SAP 账号不再出现「同步文件（N）」与「添加文件」。
- SAP 账号未配置登录信息时，详情页给出「配置 SAP 登录」入口（原 10）。
- 滚动位置：重建外壳时按 `data-scroll-key` 记下并写回 `.pane__scroll` / `.content--scroll`
  的偏移，列表、详情、设置页都不再跳回顶部（原 1、原 2）。
- 设置页补上「复制用户名 + 密码」的换行符选项（CRLF / LF / 空格）（原 4）。
- toast 恢复 `pointer-events`，点击即可关闭（原 5）。
- 进入同步文件页预取全部文件的同步计划（原 11）。
- 判定「关联关系页图标写死 SAP」其实不是问题：该页数据本来就只包含 SAP 账号（原 12）。

### 1.1.1 —— 提交 `d4ca255`，标签 `v1.1.1`（已发布 GitHub Release）

- SAP GUI 一键登录：解析 `SAPUILandscape.xml`（`Includes` 递归、`Messageservers`、`Routers`、
  `Workspaces`、`sapguiid` 引用、路由串前缀形式），通过 `sapshcut.exe` 启动；
  支持应用服务器与服务器组 / 登录组两种配置方式。
- 密码传递方式可选：剪贴板（默认，只复制密码）或命令行明文。
- 导出 `.sap` 快捷方式：与 SAP GUI 自己保存的格式一致，不含密码、不含连接串。
- 修复：登录组连接串改用 `/R/`、`/M/` 形式；始终传 `-system`；登录后把新会话窗口置前；
  审计脚本的启动竞态与 Edge 进程泄漏。

### 1.1.0 —— 提交 `73434fd`

- 新增 `TODO.md`；界面审计脚本修复（`waitForApp()`、进程树清理、`-Interactions` 开关）。
- SAP GUI 快捷登录与 `.sap` 导出的第一版（后续在 1.1.1 修正了连接串与格式）。
