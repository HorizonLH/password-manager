# SapVault 待办（1.1.x 及以后）

这份清单来自 1.0.1 的收尾审查和 1.1.x 的实测反馈。已经处理的问题不在这里重复记录；
每条都写清了「现象 → 证据 → 建议做法」，方便以后直接开工。

**版本归属**：P2 计划在 **1.1.x** 修；P3 是之后的洁癖／长期项；
最后的「1.2.x 规划」段是**新功能**（不是修补）。

**版本计划（2026-09-21 更新）**

- **1.1.1**（已发布）：SAP GUI 一键登录、`.sap` 快捷方式导出，以及连接串 / 登录窗口的一系列修复。
- **1.1.2**（本次）：账号详情两处修复（切分类残留、非 SAP 账号显示同步文件）+
  P2 的 1 / 2 / 4 / 5 / 10 / 11（滚动位置、换行符设置、toast 点击、未配置登录提示、同步计划预取）。
- **1.1.3**（计划）：P2 的 6 / 7 / 8 / 9（导入保险库弹窗、模态框焦点陷阱、编辑弹窗折叠、表单控件对齐）。
- **1.2.0**（计划）：备注里的链接可直接点击、同步文件绑定下拉的可用性（含键搜索）。
- **1.2.1 / 1.2.2**（计划）：自定义标题栏（第 18 条）、单实例运行（第 19 条）。

---

## P2

### 1. 全量重渲染会丢滚动位置 ✅ 已在 1.1.2 修复

- 现象：账号列表很长时，点一条记录、点一次收藏、切一次分类，列表都会跳回顶部；
  详情面板里「显示密码」、历史密码展开状态也会在任意一次状态变化后被重置。
- 证据：`ui/js/app.js` 的 `render()` 每次都用 `mount(root, …)` 重建整个外壳，
  `captureFocus()` 只保存焦点和光标；`ui/` 目录里没有任何 `scrollTop`/`scrollIntoView`。
- 建议：把滚动偏移纳入快照（按 `.pane__scroll` / `.content--scroll` 的选择器 + 视图名记录 `scrollTop`），
  或把账号列表改成局部重绘。详情面板的展开/显示状态同理，可提到 `state` 里。

### 2. 设置页点一次按钮就滚回顶部（1.1.0 实测反馈）✅ 已在 1.1.2 修复

- 现象：在「设置」页点任意开关、分段按钮或「立即备份」之类的按钮后，页面瞬间跳回顶部，
  要继续调下一项就得重新滚下去。
- 原因：与第 1 条同一个根因。`saveSettings()` → `setState()` → 全量 `render()`，
  而设置页的滚动容器是每次新建的 `div.content.content--scroll`（`ui/js/app.js`），`scrollTop` 归零。
- 建议：和 1 一起修（一次改动即可覆盖列表与设置页）；修完补一条交互回归：
  「设置页滚到中间 → 点一个开关 → 滚动位置不变」。

### 3. 同步文件页没有键搜索，`Ctrl+K` 指向不存在的元素

- 现象：文件里几十上百个键时只能肉眼翻找；在同步/关联页按 `Ctrl+K` 没有任何反应。
- 证据：`ui/js/app.js` 的 `bindShortcuts()` 在非账号页聚焦 `#sap-filter`，而整个前端没有这个 id。
- 建议：给「同步文件」页的键列表加一个过滤框（复用 `.search` 样式），
  把 `Ctrl+K` 指过去；关联关系页也补齐快捷键。

### 4. `sap_line_separator` 在界面上改不了 ✅ 已在 1.1.2 修复

- 现象：README 1.2 说「分隔符可在设置里选择 CRLF 或 LF」，设置页里找不到这一项。
- 证据：字段在 `src-tauri/src/store.rs` 的 `Settings` 里、被 `copy_sap_credentials`
  和 SAP 登录的剪贴板模式使用，但 `ui/js/views/settings.js` 没有任何控件写它。
- 建议：在「锁定与剪贴板」卡片里加一个二选一控件，并说明它同时影响
  「复制用户名 + 密码」和「剪贴板模式登录」。

### 5. toast 点击关闭失效 ✅ 已在 1.1.2 修复

- 现象：点提示条不会关闭（只能等它自己消失，所以一直没被发现）。
- 证据：`ui/styles/app.css` 的 `.toasts` 设了 `pointer-events: none`，`.toast` 没有重新开启，
  而 `ui/js/toast.js` 绑了 click 处理。
- 建议：在 `.toast` 上加 `pointer-events: auto`（或把 click 收到 `.toasts` 之外的处理上）。

### 6. 导入保险库用了 `window.prompt`

- 现象：导入加密保险库时弹出系统原生输入框，和应用自己的弹窗风格完全不一致，
  也没有密码强度/错误提示；`modal.js` 里现成的 `promptModal()` 从未被使用（死代码）。
- 证据：`ui/js/views/settings.js` 的导入分支调用 `window.prompt`。
- 建议：改用 `promptModal()`，并加上「密码错误」时的重试循环。

### 7. 模态框没有焦点陷阱

- 现象：弹窗打开时按 Tab 可以一路跑到背后的界面上；关闭后焦点不回到触发按钮。
- 证据：`ui/js/modal.js` 只有 `role="dialog"`、`aria-modal="true"` 和 Esc 关闭。
- 建议：打开时记录触发元素、把焦点限制在 `.modal` 内循环，关闭后还原焦点。

### 8. 编辑条目弹窗内容过多，需要折叠（1.1.0 实测反馈）

- 现象：弹窗把「基本信息 / 凭据 / SAP GUI 登录 / 密码规则 / 密码循环与历史 / 备注」
  六个区块一次性全部铺开，1240×800 下要滚很久才能看完；大部分时候只需要改标题或密码。
- 证据：`ui/js/views/editor.js` 的 `body` 里 6 个 `section(...)`；审计实测弹窗
  `.modal` 为 940×622，内容高度远超可视区。
- 建议：每个区块做成可折叠面板，默认只展开「基本信息」「凭据」；
  「SAP GUI 登录」「密码循环与历史」「密码规则」默认收起。
  展开状态要放在 `state`（或编辑器自己的持久对象）里——放在渲染函数内的局部变量会
  被下一次重渲染重置（参见第 1 条）。

### 9. 表单控件的对齐不统一（1.1.x，附 2026-09-18 的三张实测截图）

- 现象：同一个卡片里有的字段是「标签在上、控件在下」，有的是「控件 + 按钮排成一行」
  （SAP 卡片里的系统下拉、启动器路径），并且 `form__grid` 用 `auto-fit` 自动决定列数，
  于是同一行的控件左右边界、顶边与高度都可能对不齐。
- **已确认的三个具体位置**（截图核对）：
  1. **编辑条目 → 基本信息**：「标题」文本框与「分类」下拉框在同一行，但两者**顶边差约 2px、
     高度也不一致**；右列下面还有一行提示文字，导致这一行两列的标签/控件基线整体错开。
  2. **编辑条目 → 密码循环与历史 → 手动补录那一行**：「手动补录」输入框、「备注」输入框与
     「＋ 添加」按钮挤在一行：两个输入框宽度不一致，按钮比输入框**更低、更高**，
     三个控件的顶边与底边都不在同一条线上。
  3. **编辑条目 → 凭据**：「用户名」（下方还挂着「使用全局 Knox ID」复选框）与
     「密码」（右侧带眼睛按钮）两列 —— 左列多出一行复选框后，右列的密码框**整体下移**，
     两个输入框顶边不齐。
- 根因（初步）：`.form__row` 是 `display: grid` 且按内容撑高，
  同一行里只要有一列多一行内容（复选框、提示文字），另一列就会被拉偏；
  再叠加 `input` / `select` / `button` 的默认高度不完全相同（`ui/styles/app.css`）。
- 建议：
  1. 定一套规则并只用这一套：统一「标签在上、控件在下」，并给**所有可交互控件统一高度**
     （`input` / `select` / `button` 同一个 `height`、同一 `border-radius`、同一 `font-size`）。
  2. 同一行的字段按**顶对齐**（`align-items: start`），列宽交给行内网格
     （例如 `grid-template-columns: repeat(2, minmax(0, 1fr))`），不靠内容撑开。
  3. `form__grid` 的列数改成明确断点（≥720px 两列、≥1100px 三列），不再依赖 `auto-fit` 随机换行。
  4. 「输入 + 输入 + 按钮」这种行，把按钮也放进同一个网格并固定列宽，避免它自己换行落到底下。
- **顺带要做的事**：按同一套标准**全局排查**其它位置。当前布局分布可以先按这份清单过一遍：
  `form__grid` 共 12 处（`ui/js/views/editor.js` 8 处、`ui/js/views/settings.js` 3 处、
  `ui/js/views/filedialog.js` 1 处），`inline-row` 2 处（`editor.js:613`、`settings.js:317`）；
  重点看这些地方：编辑条目弹窗的 6 个区块、设置页各卡片、SAP GUI 登录区块、
  同步文件页的绑定下拉、文件上传弹窗、锁定页。每发现一处就补到这个条目下面。
- 验收方式：给 `tools/audit-ui.ps1` 加一条几何断言 —— 同一个 `.form__grid` 行内所有控件
  （`.input` / `.select` / `.btn`）的 `top` 与 `height` 差值 ≤ 1px，超了报 FAIL；
  这样以后改表单不会再悄悄退化。

### 10. 未配置 SAP 登录的账号没有任何入口提示 ✅ 已在 1.1.2 修复

- 现象：右键菜单和详情页里的「登录 SAP GUI / 导出快捷方式」只在条目已经配好
  系统 ID 或连接串时出现，所以刚升级到 1.1.0 的账号（以及还没配过的账号）看起来
  「这个功能不存在」，用户不知道要去编辑器里配置。
- 证据：`ui/js/views/accounts.js` 里详情面板的条件是
  `entry.sap?.systemId || entry.sap?.guiparm`，右键菜单的条件是
  `entry.sap?.systemId || entry.hasSapLogin`。
- 建议：SAP 分类的账号在没有配置时也显示一行提示 +「配置 SAP 登录」按钮
  （直接打开编辑器并聚焦到该区块）；或者在详情页放一枚置灰的按钮并说明原因。

### 11. 同步文件页只有在选中文件时才拉取「同步计划」✅ 已在 1.1.2 修复

- 现象：刚进入页面时除第一个文件外，其它文件的状态列显示 `—`，点「重新检测」才补齐。
- 证据：`ui/js/views/sync.js` 的 `renderSync()` 只为 `files[0]` 请求 `filePlan`。
- 建议：进入页面时统一调一次 `filePlans()`；或把首个文件的请求改成全部。

### 12. 关联关系页把账号图标写死成 SAP ❎ 不是问题（关联关系本来就只含 SAP 账号）

- 现象：自建分类下的账号在关联关系页也显示成 SAP 的服务器图标，统计文字也写成「N 个 SAP 账号」。
- 证据：`ui/js/views/associations.js` 里用了 `categoryIconName("sap")` 和固定文案。
- 建议：改用条目自己的分类。

---

## P3

### 13. 死代码与无用状态

- `ui/js/state.js` 的 `state.presets` 从 `info.presets` 取值，但后端 `Bootstrap` 结构体里
  没有这个字段，永远是 `undefined`。要么补后端，要么删掉。
- `ui/js/views/accounts.js` 导入了 `formatBytes`、`openFileKeys` 但没使用。
- `api.js` 里的 `entrySummary`、`vaultStatus`、`paths` 没有任何调用点。
- `commands.rs` 的 `clipboard_clear` 把剪贴板逻辑又抄了一遍，`clipboard.rs` 里已有现成封装。

### 14. 新建保险库页面上挂着一个无关设置

- 现象：首次创建保险库的界面上有「删除条目时二次确认」复选框，而设置页里也有同一个开关。
- 证据：`ui/js/views/lock.js` 的 `createForm()`。
- 建议：去掉，或移到设置页。

### 15. 账号列表缺少键盘导航与语义

- 现象：`role="button"` 的行只处理 Enter/Space，没有上下方向键；也没有 `aria-selected`。
- 建议：加方向键移动选中项，并把 `role` 换成 `listbox/option` 组合。

### 16. `with_vault_mut` 不是事务性的

- 现象：闭包返回 `Err` 时不会回滚内存里的改动，也不会落盘（当前调用方都是先校验后修改，
  所以还没出问题，但这是往后加功能很容易踩的坑）。
- 证据：`src-tauri/src/state.rs` 的 `with_vault_mut()`。
- 建议：改成「校验 → 克隆 → 提交」，或让闭包只声明变更。

### 17. 界面文案里的「SAP 换行格式」说明可以合并

- 「复制用户名 + 密码」在两个页面有三处不同长度的手写说明，
  等 SAP 登录功能稳定后可以统一成一句带链接的说明。

---

## 1.2.x 规划（功能，不是修补）

### 18. 自定义窗口标题栏（与应用风格统一）

- 现状：窗口用的是 Windows 自带的标题栏和最小化／最大化／关闭按钮，深色主题下与应用整体风格
  对不上（按钮是系统配色，标题栏底色也与侧边栏、主区不一致）。
- 目标：把标题栏纳入应用自己的视觉语言（同一套圆角、hover／按下反馈、图标线宽与颜色），
  **不牺牲系统行为**（窗口吸附、Win11 Snap 布局、阴影、贴边、多显示器、DPI 缩放）。
- 技术方案（Tauri v2，按推荐顺序）：
  1. **overlay 模式（建议先试）**：`tauri.conf.json` → `app.windows[0].titleBarStyle: "overlay"`，
     内容延伸到标题栏区域，**系统按钮仍然保留** —— 吸附、Snap 布局、阴影、贴边全部不受影响，
     我们只负责标题栏区域的配色与交互。改动最小、风险最低。
  2. **完全自绘**：`decorations: false`，自己画三个按钮并调用 `appWindow.minimize()` /
     `toggleMaximize()` / `close()`；拖动区用 `data-tauri-drag-region`（或 `startDragging()`），
     双击标题栏切换最大化，监听 resize 切换"最大化/还原"图标。
     **代价**：Win11 上"鼠标悬停最大化按钮弹出 Snap 布局"会丢失，要靠 `WM_NCHITTEST` /
     `HTMAXBUTTON` 的非客户区命中测试才能找回来；贴边、多显示器拖拽都要重新验证。
  3. 需要的窗口权限（`src-tauri/capabilities/default.json`）：
     `core:window:allow-minimize`、`core:window:allow-toggle-maximize`、`core:window:allow-close`、
     `core:window:allow-start-dragging`。
- 验收清单：最小化／最大化／还原／关闭、双击标题栏、拖拽移动、贴边、Snap 布局、
  DPI 100%/125%/150%、多显示器、最大化状态下的按钮位置与悬停反馈；
  并给 `tools/audit-ui.ps1` 加一条「标题栏高度 + 三个按钮命中区」的几何断言。
- 统一标题栏之后，这一条还能放什么（按价值排序，建议分批做）：
  1. **锁定按钮 + 剪贴板倒计时**：最贴合这个应用 —— 一眼看到「还有 12 秒清空剪贴板」，
     点一下立即锁库（现在要滚到侧边栏底部才能锁）。
  2. **同步状态徽标**：还有 N 处密码待同步，点一下直接进「同步文件」页。
  3. **搜索框**：把账号页的搜索挪进标题栏，列表区多出一整行给内容。
  4. **保险库标识**：当前保险库名 + 便携／本地模式，多保险库或 U 盘场景很有用。
  5. **置顶开关**（always on top）：抄密码、对照文档时实用。
  6. **主题快速切换**：目前藏在侧边栏品牌区的图标里，要悬停才看得懂。
  7. **SAP 登录状态**：是否检测到 SAP GUI、上次登录的系统与时间。
  8. **紧凑模式**开关：缩小行高，小屏幕上多显示几条。
  9. **应用菜单**（关于、快捷键说明、打开数据目录）：「数据目录」现在锁定页和设置页各有一处。
- 注意：标题栏要与「拖拽上传的整屏投放层」「右键菜单」「模态框」的层级配合好 ——
  拖动区不能吃掉按钮点击，也不能挡住投放层。

### 19. 单实例运行：第二次启动时唤起已打开的窗口（1.2.x）

- 现状：可以同时打开多个 SapVault 窗口，而且**必须**限制 —— 两个窗口共享同一个保险库文件，
  保存时各自拿着自己内存里的快照整份回写（`state.rs::with_vault_mut` → 整份重新加密写盘），
  于是「A 窗口保存」会把「B 窗口改过但还没保存的内容」覆盖掉；锁定状态、空闲计时、剪贴板
  自动清空也各算各的，容易让人分不清哪个是"真的"。
- 期望行为：同一时间只允许一个实例；已经打开时再次双击图标/快捷方式 **不再新开窗口，
  而是把已有窗口唤到前台并聚焦**（最小化状态先还原，在后台则激活）。
- 技术方案（Tauri v2）：
  1. **官方插件（推荐）**：`tauri-plugin-single-instance`。在 `lib.rs` 里
     `.plugin(tauri_plugin_single_instance::init(|app, argv, cwd| { … }))`，
     回调里对主窗口依次 `show()` → `unminimize()` → `set_focus()`（必要时短暂置顶再取消，
     当作"窗口在这里"的提示）。注意它**必须最先注册**（在其他插件之前），前端不需要额外权限声明。
  2. 备选：自己用命名互斥体（`CreateMutexW` 后判断 `ERROR_ALREADY_EXISTS`）判断重复启动，
     再用 `FindWindowW` + `SetForegroundWindow` 唤起已有窗口 —— 不引依赖，但要多写 Win32 代码。
- 实现时要处理的细节：
  - 第二实例带命令行参数时（将来可能扩展成"打开某个条目／同步某个文件"），把参数转交给第一实例执行。
  - Windows 的前台窗口锁定策略可能让 `SetForegroundWindow` 静默失败，
    需要配合 `AllowSetForegroundWindow` 或"短暂置顶 + 取消"的写法。
  - 与未来的"最小化到托盘"配合：唤起时要从托盘还原窗口。
  - 唤起窗口 ≠ 解锁：仍然要求重新输入主密码（空闲自动锁定的语义不能变）。
  - 便携模式按什么判定"同一个应用"：建议以**数据目录**为判定键（而不是可执行文件路径），
    这样同一个 exe 指向不同数据目录的两个便携实例仍可同时运行，而共享同一保险库的实例会被拦住。
- 验收：连续双击两次只出现一个窗口；第二次启动时已有窗口被激活（最小化状态会还原）；
  两个不同数据目录的便携实例仍可各自运行；用审计/手动确认锁定时长与剪贴板倒计时不再被多实例干扰。

---

## 已在 1.1.0 处理

### 审计算子（原 P1）

- **启动竞态**：脚本只等 DevTools 端口就绪就开始测量，此时页面还没解析完，
  `document.getElementById("app")` 是 `null`，于是必然报
  `inspect-ui failed: TypeError: Cannot read properties of null (reading 'querySelector')`。
  现在 `tools/inspect-ui.mjs` 里有 `waitForApp()`，所有模式都会先等到界面渲染完成（`eval` 除外）。
- **泄漏 headless Edge**：原来只 `Kill()` 父进程，渲染/GPU 子进程继续持有临时 profile，
  于是 `finally` 里的删除静默失败、每跑一次多留一套进程（这台机器上累积过 37 个）。
  现在 `tools/audit-ui.ps1` 按 `--user-data-dir` 结束整棵进程树，并重试删除 profile，
  失败时给出警告。
- 顺手把交互回归并进同一个脚本：`pwsh tools/audit-ui.ps1 -Interactions`。

### SAP GUI 快捷登录（新功能）

- 解析 `SAPUILandscape.xml`（含 `Includes` 递归、`Messageservers`、`Routers`、`Workspaces`、
  `sapguiid` 引用、路由器前缀形式），按 SAP 官方 *SAP UI Landscape* 文档实现。
- 通过 SAP 自带的 `sapshcut.exe` 启动 SAP GUI，导出 `.sap` 快捷方式（不含密码）。
- 密码传递方式二选一：命令行明文（一次点击）或剪贴板粘贴（默认，更安全）。
- 详见 README 的 1.6 与 2.6 两节。
